# Video Assets

Place `title.mp4` here to show looping video in the title screen media frame.

Supported formats depend on Windows Media Foundation codecs, with MP4 and WMV as the intended targets. If the file is missing or decoding fails, the game keeps running and shows the fallback title media panel.
